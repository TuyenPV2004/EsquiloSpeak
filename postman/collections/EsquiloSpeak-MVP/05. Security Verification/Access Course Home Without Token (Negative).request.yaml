$kind: http-request
url: "{{baseUrl}}/courses/{{courseId}}/home"
method: GET
headers:
  Authorization: "{{bearer_token_029x}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 401", function () {
          pm.response.to.have.status(401);
      });
    language: text/javascript
order: 1000
